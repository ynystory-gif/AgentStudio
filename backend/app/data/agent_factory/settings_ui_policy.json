{
  "version": "1.0",
  "id": "SETTINGS_UI_POLICY",
  "purpose": "Agent별 필요한 설정만 React 설정 화면으로 자동 구성",
  "rules": [
    "설정 카테고리별 Section/Tab을 만든다.",
    "enum은 select/radio, bool은 toggle, secret은 password input을 우선한다.",
    "저장 전 client-side validation과 backend validation을 모두 고려한다.",
    "저장 성공/실패 상태를 사용자에게 명확히 표시한다.",
    "Secret은 화면 재진입 시 원문이 아닌 마스킹 상태만 표시한다.",
    "현재 Agent에서 사용하지 않는 기능의 설정 Section은 생성하지 않는다."
  ]
}