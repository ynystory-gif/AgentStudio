# v5.144 Agent Design Left Panel Cleanup

## 에이전트 설계 탭

기존 왼쪽 `프로젝트` 영역은 에이전트 설계 탭에서 표시하지 않습니다.

그 자리에 다음 신규 Agent 설계 단계를 표시합니다.

- 목적
- 기능
- MCP / Tool
- 실행 환경
- 확인

따라서 중앙 영역은 `Agent 설계 인터뷰` 대화 화면만 사용합니다.

다른 탭에서는 기존 프로젝트 목록/검색/Git 영역을 그대로 유지합니다.

## 글로벌 좌측 아이콘

기존에는:
- ✦ 신규 Agent 설계
- ▣ Workspace

두 아이콘이 비슷한 진입 역할을 하여 중복으로 보였습니다.

v5.144에서는 `✦ 신규 Agent 설계` 아이콘만 남기고
`▣ Workspace` 글로벌 아이콘을 제거했습니다.
