# v5.211 Brand Header Assets Fix

- AgentStudio 좌측 상단의 기존 보라색 `A` 마크를 사용자 제공 THEANOVA 심볼마크 이미지로 교체했습니다.
- 기존 텍스트 `THEANOVA`는 사용자 제공 THEANOVA 워드마크 이미지로 교체했습니다.
- 제품명 `AgentStudio` 텍스트는 기존처럼 유지합니다.
- 최종 상단 브랜드 구성은 `심볼마크 + THEANOVA 워드마크 + AgentStudio` 입니다.
- 원본 브랜드 이미지는 `frontend/public/branding/`에 포함하며 Header에서는 정적 asset으로 사용합니다.
